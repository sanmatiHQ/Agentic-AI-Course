"""Standalone assignment app — run with: streamlit run assignments/_template/app.py"""

import streamlit as st

st.set_page_config(page_title="Standalone Assignment", page_icon="🧪")
st.title("Standalone assignment")
st.write("Use this when an assignment should run as its own app, not in the multipage hub.")
